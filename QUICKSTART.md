# 🚀 БЪРЗО СТАРТИРАНЕ - ЕДНО КЛИКВАНЕ

## ⚡ Как да стартирам приложението?

### Windows

1. **Изтегли проекта** (ако вече не е изтеглен):
   ```powershell
   git clone https://github.com/reminchev/lidl-receipt-downloader.git
   cd lidl-receipt-downloader
   ```

2. **Стартирай с двоен клик** на един от тези файлове:
   - `start.bat` (обикновен двоен клик)
   - `start.ps1` (десен клик → Run with PowerShell)

### Linux/macOS

1. **Изтегли проекта** (ако вече не е изтеглен):
   ```bash
   git clone https://github.com/reminchev/lidl-receipt-downloader.git
   cd lidl-receipt-downloader
   ```

2. **Стартирай:**
   ```bash
   chmod +x start.sh
   ./start.sh
   ```

---

## ✨ Какво се случва автоматично?

Скриптът `start.bat` / `start.ps1` / `start.sh` **автоматично**:

1. ✅ Проверява дали Python е инсталиран
2. ✅ Създава виртуална среда (ако не съществува)
3. ✅ Инсталира всички нужни библиотеки (ако не са инсталирани)
4. ✅ Инсталира Playwright браузъра (ако не е инсталиран)
5. ✅ Стартира приложението

**При първо стартиране:** Процесът може да отнеме 2-5 минути за инсталация.

**При следващи стартирания:** Приложението стартира веднага (няколко секунди).

---

## ❓ Какво трябва да имам инсталирано преди това?

**Само Python 3.8 или по-нов!**

- **Windows:** Изтегли от [python.org](https://www.python.org/downloads/)
- **Linux:** `sudo apt install python3 python3-venv` (Ubuntu/Debian)
- **macOS:** `brew install python3` или от [python.org](https://www.python.org/downloads/)

Всичко останало се инсталира автоматично! 🎉

---

## 🔧 Старите скриптове (ако предпочитате ръчна инсталация)

Ако искаш да раздлиш процеса на инсталация и стартиране:

### Windows:
```powershell
.\setup.ps1   # Инсталация (веднъж)
.\run.bat     # Стартиране
```

### Linux/macOS:
```bash
./setup.sh    # Инсталация (веднъж)
./run.sh      # Стартиране
```

---

## 💡 Препоръки

✅ **Използвай `start.bat` или `start.ps1`** - най-лесният начин!

❌ Не е нужно да изпълняваш `setup.ps1` преди това

❌ Не е нужно да активираш виртуална среда ръчно

❌ Не е нужно да инсталираш библиотеки ръчно

**Просто стартирай `start.bat` и всичко става автоматично!** 🚀
